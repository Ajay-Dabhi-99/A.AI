<!-- Title must start with the task ID, e.g. "MODEL-011 Add normalized chat streaming". -->

## Task

MODEL-XXX · Phase P? · Closes #

## Behavior added

-

## Changed files / modules

-

## Tests executed

- [ ] `pnpm typecheck` → PASS
- [ ] `pnpm lint` → PASS
- [ ] `pnpm test:unit` → PASS
- [ ] `pnpm test:integration` → PASS
- [ ] `pnpm test:live` → PASS (required when DB/Redis behavior changes)
- [ ] `pnpm build` → PASS
- [ ] E2E / manual QA:

No test is skipped "to be fixed later" (blueprint §21.9).

## Migration impact

N/A, or: migration name, reproducible from a clean database? Supabase RLS applied to new tables?

## Environment variable changes

N/A, or: names added/changed (also updated in `.env.example` and `packages/config`).

## API contract changes

N/A, or: `docs/api` and `packages/shared-types` updated in this PR.

## Known issues

NONE, or issue IDs with severity.
